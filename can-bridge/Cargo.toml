[package]
name = "can-bridge"
version = "0.1.0"
edition = "2021"

[dependencies]
nusb = "0.1"
futures-lite = "2"
tokio = { version = "1", features = ["net", "rt-multi-thread", "io-util", "sync", "macros", "signal"] }
hex = "0.4.3"
